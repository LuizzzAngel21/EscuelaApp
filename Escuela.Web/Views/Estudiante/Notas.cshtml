@{
    ViewData["Title"] = "Historial Académico";
    Layout = "~/Views/Shared/_LayoutEstudiante.cshtml";
}

<div class="container-fluid py-4">

    <div class="row mb-4 align-items-center justify-content-between">
        <div class="col-md-6">
            <h2 class="fw-bold text-dark mb-1">Historial Académico</h2>
            <p class="text-muted mb-0">Resumen de rendimiento actual.</p>
        </div>
        <div class="col-md-6 text-end">
            <button onclick="descargarPDF()" class="btn btn-primary rounded-pill px-4 shadow-sm">
                <i class="fa-solid fa-file-pdf me-2"></i>Descargar Boleta
            </button>
        </div>
    </div>

    <div id="areaImprimible" class="p-2 bg-white">

        <div class="d-none d-print-block mb-4 text-center" id="cabeceraPdf">
            <h3 class="fw-bold">INSTITUCIÓN EDUCATIVA ALFRED NOBEL</h3>
            <p class="small text-muted">Reporte Oficial de Calificaciones - Periodo 2026</p>
            <hr />
        </div>

        <div class="card border-0 shadow-none mb-4 bg-light">
            <div class="card-body d-flex align-items-center justify-content-between p-3">
                <div>
                    <h6 class="text-secondary text-uppercase small mb-1">Promedio General Ponderado</h6>
                    <h2 class="fw-bold mb-0 text-primary" id="lblPromedioGeneral">--</h2>
                </div>
                <div class="display-4 opacity-25 text-primary">
                    <i class="fa-solid fa-chart-pie"></i>
                </div>
            </div>
        </div>

        <div class="card border-0 shadow-sm overflow-hidden">
            <div class="card-body p-0">
                <table class="table table-hover align-middle mb-0">
                    <thead class="bg-primary text-white">
                        <tr>
                            <th class="ps-4 py-3 small text-uppercase fw-bold">Curso</th>
                            <th class="py-3 small text-uppercase fw-bold">Docente</th>
                            <th class="text-center py-3 small text-uppercase fw-bold">Estado</th>
                            <th class="text-center py-3 small text-uppercase fw-bold">Avance</th>
                            <th class="text-end pe-4 py-3 small text-uppercase fw-bold">Promedio</th>
                        </tr>
                    </thead>
                    <tbody id="tablaBoleta">
                        <tr>
                            <td colspan="5" class="text-center py-5">
                                <div class="spinner-border text-primary" role="status"></div>
                                <p class="text-muted small mt-2">Consultando registros...</p>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="mt-4 text-center text-muted small d-none d-print-block" id="piePdf">
            <p>Este documento es de carácter informativo. Para trámites oficiales solicite sello en secretaría.</p>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>

    <script src="~/js/estudiante/notas.js"></script>
}